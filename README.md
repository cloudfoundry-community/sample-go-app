A sample go application that supports the ocf-scheduler test.

Use the append the /call to your cf app route.  The scheduler
will be calling the the endpoint of /call/.  This is the default
behavior for now for the ocf scheduler.
